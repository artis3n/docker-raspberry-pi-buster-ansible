command:
  python3 --version:
    exit-status: 0
    stdout:
      - Python 3.
  ansible --version:
    exit-status: 0
    stdout:
      - executable location = /usr/sbin/ansible
    stderr: []
    timeout: 15000  # in milliseconds
  systemctl status --no-pager:
    exit-status: 0
